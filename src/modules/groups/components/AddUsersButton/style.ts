import { DialogActions as BaseDialogActions, styled } from '@mui/material'

export const DialogActions = styled(BaseDialogActions)({
  display: 'flex',
  justifyContent: 'space-between',
  alignContent: 'center',
})
