const paths = {
  root: '/',
  login: '/login',
  register: '/register',
  groups: '/groups',
  group: '/group/:groupId',
  signIn: '/sign-in',
  signUp: '/sign-up',
  // <-- paths-end -->
}

export default paths
