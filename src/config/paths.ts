const paths = {
  root: '/',
  login: '/login',
  register: '/register',
  groups: '/groups',
  group: '/group/:groupId',
  userInfo: '/user-info',
  signIn: '/sign-in',
  signUp: '/sign-up',
  // <-- paths-end -->
}

export default paths
